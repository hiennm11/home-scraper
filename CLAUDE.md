## Agent skills

### Issue tracker

GitHub Issues — `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Dùng label default (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` ở root. See `docs/agents/domain.md`.

version: '3.8'

services:
  scraper:
    image: scraper:local
    container_name: scraper
    restart: unless-stopped
    ports:
      - "8899:8899"
    networks:
      - homelab_net

networks:
  homelab_net:
    external: true